import User from './User';

class Server {
    constructor(name:string, ip:string, port:number) {
        this.name = name;
        this.ip = ip;
        this.port = port;
    }
}

export default Server;
