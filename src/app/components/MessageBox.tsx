jmport React, { useState } from 'react';
import ReplyButton from './ReplyButton';

const MessageBox = () => {
    const sendMessage = () => {
        console.log("hi");
        setText('');
    }

    return (
    );
};

export default MessageBox;

